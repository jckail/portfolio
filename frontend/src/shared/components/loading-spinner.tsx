import React from 'react';

/** Centered spinner used as the loading fallback for lazy sections. */
export const LoadingSpinner: React.FC = () => (
  <div className="section-loading">
    <div className="loading-spinner"></div>
  </div>
);
